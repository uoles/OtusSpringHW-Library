spring:
  banner:
    location: "classpath:banner-test.txt"

  datasource:
    initialization-mode: always
    platform: test

  jpa:
    hibernate:
      ddl-auto: none

    properties:
      hibernate:
        show_sql: true
        format_sql: true

  liquibase:
    enabled: true