spring:
  banner:
    location: "classpath:banner.txt"

  datasource:
    initialization-mode: always
    platform: test

  jpa:
    hibernate:
      ddl-auto: none

    properties:
      hibernate:
        show_sql: false
        format_sql: false

